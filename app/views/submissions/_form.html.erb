<%= simple_form_for [parent, resource] do |f| %>
  <%= f.input :compiler_id, collection: Compiler.where(status: :public), include_blank: false %>

  <%= f.input :source %>

  <%= f.submit class: 'btn btn-default' %>
<% end %>
