<%= simple_form_for [parent, resource] do |f| %>
  <%= f.input :compiler, collection: Compiler::ALL.keys, include_blank: false %>

  <%= f.input :source %>

  <%= f.submit class: 'btn btn-default' %>
<% end %>
