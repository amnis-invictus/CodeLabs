<%= simple_form_for [parent, resource] do |f| %>
  <%= f.input :compiler_id, collection: visible_compilers, include_blank: false %>

  <%= f.input :source %>

  <%= f.submit class: 'btn btn-primary' %>
<% end %>
