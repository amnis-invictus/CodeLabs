<%= simple_form_for [parent, resource], remote: true do |f| %>
  <%= f.hidden_field :type, value: 'request' %>

  <%= f.submit t('membershipable.new.request'), class: 'btn btn-primary btn-block' %>
<% end %>
